token.json
google_token.json
onedrive_token.json
backup_store/
restore_out/
.envrc
.idea/
/bin/
# Built by scripts/benchmark/memory.sh; only cloudstic used to be ignored, so
# benchreport and gentree landed in the working tree — and, once, in a commit.
/gentree
/benchreport
# Ad-hoc binaries from `go build -o <name> ./cmd/cloudstic` while benchmarking.
# Two of these (cs, cs439, ~90 MB) reached main before this rule existed.
/cs
/cs*[0-9]
# `go build ./...` run from inside cmd/cloudstic writes the binary next to the
# source. Every rule above is root-anchored, so nothing caught it.
/cmd/cloudstic/cloudstic
dist/

# Test output
coverage.out
test-output.json
*.test
*.test.exe
*.prof
.DS_Store
# opencode
.opencode/
